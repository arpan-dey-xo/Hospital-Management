spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_db
    username: root
    password: MidEclipse$090104
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
